spring:
  kafka:
    bootstrap-servers: "localhost:9092"
    consumer:
      group-id: "my-group"
      auto-offset-reset: "earliest"
    template:
      default-topic: "my-topic"
topics:
  jobs-topic: "parsed-offers"